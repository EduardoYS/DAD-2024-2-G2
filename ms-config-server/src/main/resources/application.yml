server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/EduardoYS/DAD-2024-2-G2 # ruta del repository git
          searchPaths: config-data
          default-label: main
  security:
    user:
      name: root
      password: s3cr3t
